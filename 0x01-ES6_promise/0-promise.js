export default function getResponseFromAPI() {
  return new Promise(() => {
    // Your API logic here
    // Call resolve() if the API call is successful
    // Call reject() if there is an error
  });
}
